
# Plotting box plots for each numeric variable separately
for column in numeric_df.columns:
    plt.figure(figsize=(6, 4))
    plt.boxplot(numeric_df[column].dropna())
    plt.title(f'Box Plot of {column}')
    plt.ylabel(column)
    plt.show()
